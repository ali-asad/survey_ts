import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0d1a26",
    },
    secondary: {
      main: "#f5f8fa",
    }
  }
});

export default theme;
